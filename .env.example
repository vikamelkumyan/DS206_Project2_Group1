# Local SQL Server secrets only.
# Put server, database, driver, auth mode, and user settings in:
# infrastructure_initiation/sql_server_config.cfg
# .env is gitignored and should never be committed.

MSSQL_PASSWORD=your_strong_password_here
